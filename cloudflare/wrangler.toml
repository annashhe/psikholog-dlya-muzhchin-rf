name = "psi-leads"
main = "psi-leads-worker.js"
compatibility_date = "2024-01-01"

# Dashboard / wrangler secrets:
#   BOT_TOKEN, CHAT_ID
#   LEADS_INGEST_SECRET  (same as VPS backend .env — header X-Leads-Secret)
# Optional vars:
#   BACKEND_LEADS_URL = https://anna-backend.ru/public/leads
#   TURNSTILE_SECRET_KEY
#
# Deploy: npx wrangler deploy
# Secret: npx wrangler secret put LEADS_INGEST_SECRET
